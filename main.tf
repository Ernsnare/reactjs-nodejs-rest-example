provider "generic" {
  ip_address = "172.17.32.96"  # Ganti dengan alamat IP server on-premise Anda
  ssh_key_file = "~/.ssh/id_rsa"  # Ganti dengan path ke kunci SSH Anda
  ssh_username = "root"  # Ganti dengan username SSH Anda
  ssh_password = "seninselasa73" # Password ssh
}
